{
  "name": "event-management-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "vercel-build": "cd backend && npm install && npm run build"
  }
}
